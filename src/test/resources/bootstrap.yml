logging:
  level:
    ROOT: 'ERROR'

server:
  port: 0

spring:
  cloud:
    config:
      override-none: true
  main:
    banner-mode: 'off'
  output:
    ansi:
      enabled: ALWAYS
  zipkin:
    enabled: false
